import re
import emoji
from termcolor import colored
from modules.connection import irc_socket
from modules.user_actions import send_message

# Funções para tratamento de mensagens, emojis, notificações, etc.
def format_message(user, decrypted_message):
    # Resto do código para formatação da mensagem...

def notify_mention(user, message):
    # Resto do código para notificação de menção...
